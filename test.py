# from evaluate import matriz

# # Completa el ejercicio aquí
# for i1, e1 in enumerate(matriz):
    # for i2, e2 in enumerate(e1):
        # for i3, e3 in enumerate(e2):
            # if e3 % 2 == 0:
                # matriz[i3,i2,i1]=0
            # else:
                # matriz[i3,i2,i1]=1
                
# print(matriz)





# for i, tabla in enumerate(matriz):
    # for j, fila in enumerate(tabla):
        # for k, celda in enumerate(fila):
            # if celda % 2 == 0:
                # matriz[i][j][k] = 0
            # else:
                # matriz[i][j][k] = 1
            

print("Test")